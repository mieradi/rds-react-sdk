import styled from 'styled-components';
/**
 * @name FormWarningStyles
 * @desc description here
 * @param {} param desc
 * @returns returns desc
 */

interface FormWarningStylesProps {}

export const FormWarningStyles = styled.span<FormWarningStylesProps>`
  display: block;
  font-weight: bold;
  color: var(--red);
  margin-top: 10px;
`;
